[project]
name = "go2-backflip"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "casadi>=3.8.1",
    "drake>=1.56.0",
    "mjlab>=1.6.0",
]

[project.entry-points."mjlab.tasks"]
go2_backflip = "go2_backflip.rl"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/go2_backflip"]
